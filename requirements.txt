# ── YouTube API ────────────────────────────────────────────────────────────────
google-api-python-client>=2.120.0

# ── Image processing ───────────────────────────────────────────────────────────
Pillow>=10.3.0
opencv-python-headless>=4.9.0
imagehash>=4.3.1
numpy>=1.26.0
requests>=2.31.0

# ── Segmentation (default: rembg — works on macOS/Linux/Win, CPU-only) ─────────
rembg>=2.0.50

# MediaPipe (alternative backend; NOTE: mp.solutions.selfie_segmentation was
# removed in MediaPipe ≥ 0.10 on macOS — rembg is used as automatic fallback)
mediapipe>=0.10.14

# SAM 2 (GPU, highest quality) — install manually from:
#   https://github.com/facebookresearch/segment-anything-2
# Also needs: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Face compositing ───────────────────────────────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.18.0          # CPU inference for InsightFace
# onnxruntime-gpu>=1.18.0    # uncomment for GPU inference

# ── Web app ────────────────────────────────────────────────────────────────────
flask>=3.0.0

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
